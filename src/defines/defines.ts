export enum EDialogType {
    ALERT,
    CONFIRM,
}

export enum ENavigationType {
    ENTIRE = 'entire',
    ME = 'me',
    OTHER = 'other',
}

export enum EYear {
    MIN_YEAR = 1950,
    MAX_YEAR = 2050,
}

export enum EArrowDirection {
    LEFT,
    RIGHT,
}
