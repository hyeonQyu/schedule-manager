import React from 'react';
import { observer } from 'mobx-react';
import Layout from '@components/layout/Layout';

const OtherSchedule = observer(() => {
    return <Layout></Layout>;
});

export default OtherSchedule;
