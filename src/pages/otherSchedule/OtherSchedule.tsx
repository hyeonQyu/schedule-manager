import React from 'react';
import { RouteComponentProps } from 'react-router-dom';
import Layout from '@components/layout/Layout';

const OtherSchedule = (props: RouteComponentProps) => {
    return <Layout {...props} />;
};

export default OtherSchedule;
